# Persistent Segment Tree Auditor
**Versioned range min/max queries** - IITH SURE 2026

Built for audit trails/blockchain logging. 
- Lazy propagation range updates
- 50+ version history (non-mutating)
- O(n log n) space, O(log n) query/update
